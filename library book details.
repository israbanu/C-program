#include <stdio.h>

struct Book {
    int bookId;
    char title[50];
    char author[50];
    float price;
};

int main() {
    struct Book b;

    // Input book details
    printf("Enter Book ID: ");
    scanf("%d", &b.bookId);

    printf("Enter Book Title: ");
    scanf(" %[^\