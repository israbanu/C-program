#include <stdio.h>

int main() {
    int n, i;

    printf("Enter number of students: ");
    scanf("%d", &n);

    printf("Enter attendance numbers:\n");

    for(i = 1; i <= n; i++) {
        int att;
        printf("Student %d attendance number: