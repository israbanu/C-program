#include <stdio.h>

struct Student {
    int id;
    char name[50];
    float marks;
};

int main() {
    struct Student s[10];   // Max size 10
    int i, n, add;

    // Existing records
    printf("Enter number of existing records: ");
    scanf("%d", &n);